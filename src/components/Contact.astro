<section class="contact" id="kontakt">
  <div class="contact__inner">

    <div class="contact__header reveal">
      <span class="contact__eyebrow">07 / KONTAKT</span>

      <h2>
        Masz projekt?<br />
        <span>Zróbmy go razem.</span>
      </h2>
    </div>

    <div class="contact__bottom reveal">
      <div class="contact__description">
        <p>
          Opowiedz nam, czego potrzebujesz. Doradzimy technologię,
          przygotujemy projekt i zajmiemy się produkcją
          od początku do końca.
        </p>
      </div>

      <a class="contact__cta" href="mailto:kontakt@studio306.pl">
        <span>Napisz do nas</span>
        <span class="contact__arrow">↗</span>
      </a>
    </div>

    <div class="contact__details reveal">

      <div class="contact__detail">
        <span class="contact__label">E-MAIL</span>
        <a href="mailto:kontakt@studio306.pl">
          kontakt@studio306.pl
        </a>
      </div>

      <div class="contact__detail">
        <span class="contact__label">TELEFON</span>
        <a href="tel:+48123456789">
          +48 123 456 789
        </a>
      </div>

      <div class="contact__detail">
        <span class="contact__label">STUDIO</span>
        <p>
          Warszawa
        </p>
      </div>

    </div>

  </div>
</section>

<style>
  .contact {
    padding: 240px 0 110px;

    background:
      linear-gradient(
        180deg,
        #101820 0px,
        #26313a 90px,
        #697077 170px,
        #d9d9d7 250px,
        #f7f5f2 350px
      );

    color: #101820;
  }

  .contact__inner {
    width: min(1400px, calc(100% - 80px));
    margin: 0 auto;
  }

  /* =========================
     HEADER
     ========================= */

  .contact__header {
    padding-top: 160px;
  }

  .contact__eyebrow {
    display: block;

    margin-bottom: 28px;

    color: #555;

    font-size: 1rem;
    font-weight: 700;
    letter-spacing: 0.14em;
  }

  .contact h2 {
    margin: 0;

    max-width: 1250px;

    color: #101820;

    font-size: clamp(72px, 9.5vw, 150px);
    line-height: 0.86;
    letter-spacing: -0.065em;

    font-weight: 700;
  }

  .contact h2 span {
    color: rgba(16, 24, 32, 0.34);
  }

  /* =========================
     CTA
     ========================= */

  .contact__bottom {
    display: grid;

    grid-template-columns:
      minmax(280px, 0.75fr)
      minmax(500px, 1.25fr);

    gap: 100px;

    align-items: end;

    margin-top: 100px;
  }

  .contact__description {
    max-width: 430px;
  }

  .contact__description p {
    margin: 0;

    color: #4d5358;

    font-size: 18px;
    line-height: 1.6;
  }

  .contact__cta {
    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 28px 0;

    border-top: 1px solid rgba(16, 24, 32, 0.25);
    border-bottom: 1px solid rgba(16, 24, 32, 0.25);

    color: #101820;
    text-decoration: none;

    font-size: clamp(32px, 4vw, 58px);
    line-height: 1;

    letter-spacing: -0.04em;
    font-weight: 500;

    transition:
      padding 0.35s cubic-bezier(0.22, 1, 0.36, 1),
      border-color 0.35s ease;
  }

  .contact__cta:hover {
    padding-left: 20px;
    padding-right: 20px;

    border-color: #101820;
  }

  .contact__arrow {
    display: flex;
    align-items: center;
    justify-content: center;

    width: 64px;
    height: 64px;

    flex-shrink: 0;

    border: 1px solid rgba(16, 24, 32, 0.35);
    border-radius: 50%;

    font-size: 24px;

    transition:
      transform 0.35s cubic-bezier(0.22, 1, 0.36, 1),
      background 0.35s ease,
      color 0.35s ease;
  }

  .contact__cta:hover .contact__arrow {
    transform: rotate(45deg);

    background: #101820;
    color: #f7f5f2;
  }

  /* =========================
     DETAILS
     ========================= */

  .contact__details {
    display: grid;

    grid-template-columns: repeat(3, 1fr);

    gap: 40px;

    margin-top: 150px;
    padding-top: 30px;

    border-top: 1px solid rgba(16, 24, 32, 0.16);
  }

  .contact__detail {
    display: flex;
    flex-direction: column;

    gap: 10px;
  }

  .contact__label {
    color: #777;

    font-size: 12px;
    font-weight: 700;

    letter-spacing: 0.12em;
  }

  .contact__detail a,
  .contact__detail p {
    margin: 0;

    color: #101820;

    font-size: 18px;
    line-height: 1.4;

    text-decoration: none;
  }

  .contact__detail a {
    width: fit-content;
  }

  .contact__detail a:hover {
    text-decoration: underline;
  }

  /* =========================
     TABLET
     ========================= */

  @media (max-width: 900px) {
    .contact {
      padding: 180px 0 80px;
    }

    .contact__inner {
      width: min(100% - 36px, 1400px);
    }

    .contact__header {
      padding-top: 120px;
    }

    .contact h2 {
      font-size: clamp(60px, 13vw, 100px);
    }

    .contact__bottom {
      grid-template-columns: 1fr;

      gap: 50px;

      margin-top: 70px;
    }

    .contact__description {
      max-width: 600px;
    }

    .contact__details {
      margin-top: 100px;
    }
  }

  /* =========================
     MOBILE
     ========================= */

  @media (max-width: 600px) {
    .contact {
      padding: 150px 0 60px;

      background:
        linear-gradient(
          180deg,
          #101820 0px,
          #26313a 70px,
          #697077 130px,
          #d9d9d7 190px,
          #f7f5f2 270px
        );
    }

    .contact__header {
      padding-top: 110px;
    }

    .contact__eyebrow {
      margin-bottom: 20px;
    }

    .contact h2 {
      font-size: clamp(54px, 15vw, 76px);

      line-height: 0.9;
    }

    .contact__bottom {
      margin-top: 60px;
    }

    .contact__cta {
      padding: 22px 0;

      font-size: 34px;
    }

    .contact__arrow {
      width: 52px;
      height: 52px;

      font-size: 20px;
    }

    .contact__details {
      grid-template-columns: 1fr;

      gap: 36px;

      margin-top: 90px;
    }
  }

@media (max-width: 700px) {

.contact { padding: 56px 0; background: #f7f5f2; }
.contact__header { padding-top: 0; }
.contact h2 { font-size: 2.75rem; line-height: 1.02; }
.contact__bottom { margin-top: 28px; }
.contact__cta { font-size: 1.8rem; }
.contact__details { margin-top: 36px; gap: 24px; }
.contact__details a { overflow-wrap: anywhere; }

}
</style>