<section class="projects-section" id="realizacje">
  <div class="projects-container">
    <div class="projects-heading reveal">
      <span class="projects-eyebrow">05 / REALIZACJE</span>

      <h2>
        Zobacz, jak nasze projekty<br />
        wyglądają w rzeczywistości.
      </h2>
    </div>

    <div class="projects-grid">

      <!-- 01 / DUŻY KAFEL -->
      <a href="#" class="project-card project-card--hero reveal">
        <img
          src="/images/realizacje/witryny.jpg"
          alt="Realizacja witryny reklamowej"
        />
        <div class="project-overlay"></div>

        <div class="project-content">
          <span class="project-number">01</span>

          <div class="project-bottom">
            <h3>Witryny</h3>

            <span class="project-link">
              Zobacz galerię
              <span class="project-arrow">↗</span>
            </span>
          </div>
        </div>
      </a>

      <!-- 02 -->
      <a href="#" class="project-card reveal">
        <img
          src="/images/realizacje/samochody.jpg"
          alt="Oklejony samochód"
        />
        <div class="project-overlay"></div>

        <div class="project-content">
          <span class="project-number">02</span>

          <div class="project-bottom">
            <h3>Samochody</h3>

            <span class="project-link">
              Zobacz galerię
              <span class="project-arrow">↗</span>
            </span>
          </div>
        </div>
      </a>

      <!-- 03 -->
      <a href="#" class="project-card reveal">
        <img
          src="/images/realizacje/banery.jpg"
          alt="Baner reklamowy"
        />
        <div class="project-overlay"></div>

        <div class="project-content">
          <span class="project-number">03</span>

          <div class="project-bottom">
            <h3>Banery</h3>

            <span class="project-link">
              Zobacz galerię
              <span class="project-arrow">↗</span>
            </span>
          </div>
        </div>
      </a>

      <!-- 04 -->
      <a href="#" class="project-card reveal">
        <img
          src="/images/realizacje/pos.jpg"
          alt="Materiały POS"
        />
        <div class="project-overlay"></div>

        <div class="project-content">
          <span class="project-number">04</span>

          <div class="project-bottom">
            <h3>POS / displaye</h3>

            <span class="project-link">
              Zobacz galerię
              <span class="project-arrow">↗</span>
            </span>
          </div>
        </div>
      </a>

      <!-- 05 -->
      <a href="#" class="project-card reveal">
        <img
          src="/images/realizacje/oznakowanie.jpg"
          alt="Kasetony i oznakowanie"
        />
        <div class="project-overlay"></div>

        <div class="project-content">
          <span class="project-number">05</span>

          <div class="project-bottom">
            <h3>Kasetony i oznakowanie</h3>

            <span class="project-link">
              Zobacz galerię
              <span class="project-arrow">↗</span>
            </span>
          </div>
        </div>
      </a>

      <!-- 06 / SZEROKI KAFEL -->
      <a href="#" class="project-card project-card--bottom reveal">
        <img
          src="/images/realizacje/papierowe.jpg"
          alt="Materiały papierowe"
        />
        <div class="project-overlay"></div>

        <div class="project-content">
          <span class="project-number">06</span>

          <div class="project-bottom">
            <h3>Materiały papierowe</h3>

            <span class="project-link">
              Zobacz galerię
              <span class="project-arrow">↗</span>
            </span>
          </div>
        </div>
      </a>

    </div>
  </div>
</section>

<style>
  .projects-section {
    padding: 130px 0;
    background: #f7f5f2;
  }

  .projects-container {
    width: min(1400px, calc(100% - 80px));
    margin: 0 auto;
  }

  .projects-heading {
    margin-bottom: 64px;
  }

  .projects-eyebrow {
    display: block;
    margin-bottom: 18px;

    color: #555;

    font-size: 1rem;
    font-weight: 700;
    letter-spacing: 0.14em;
  }

  .projects-heading h2 {
    max-width: 1050px;
    margin: 0;

    font-size: clamp(48px, 5.8vw, 86px);
    line-height: 0.98;
    letter-spacing: -0.045em;
    font-weight: 700;

    color: #101820;
  }

  /* =========================
     GRID — WARIANT B
     ========================= */

.projects-grid {
  display: grid;
  grid-template-columns: repeat(9, minmax(0, 1fr));
  grid-template-rows: 250px 250px;
  gap: 18px;
}

.project-card--hero {
  grid-column: 1 / 4;
  grid-row: 1 / 3;
}

.projects-grid .project-card:nth-child(2) {
  grid-column: 4 / 7;
  grid-row: 1;
}

.projects-grid .project-card:nth-child(3) {
  grid-column: 7 / 10;
  grid-row: 1;
}

.projects-grid .project-card:nth-child(4) {
  grid-column: 4 / 6;
  grid-row: 2;
}

.projects-grid .project-card:nth-child(5) {
  grid-column: 6 / 8;
  grid-row: 2;
}

.project-card--bottom {
  display: block;
  grid-column: 8 / 10;
  grid-row: 2;
}

  /* =========================
     KAFLE
     ========================= */

  .project-card {
    position: relative;
    display: block;

    overflow: hidden;
    border-radius: 18px;

    text-decoration: none;
    color: white;

    background: #18232c;
  }

  .project-card img {
    position: absolute;
    inset: 0;

    width: 100%;
    height: 100%;

    object-fit: cover;

    transition:
      transform 0.8s cubic-bezier(0.22, 1, 0.36, 1),
      filter 0.6s ease;
  }

  .project-card:hover img {
    transform: scale(1.045);
  }

  .project-overlay {
    position: absolute;
    inset: 0;

    background: linear-gradient(
      180deg,
      rgba(4, 12, 19, 0.08) 15%,
      rgba(4, 12, 19, 0.15) 50%,
      rgba(4, 12, 19, 0.78) 100%
    );

    transition: background 0.5s ease;
  }

  .project-card:hover .project-overlay {
    background: linear-gradient(
      180deg,
      rgba(4, 12, 19, 0.16) 0%,
      rgba(4, 12, 19, 0.28) 45%,
      rgba(4, 12, 19, 0.86) 100%
    );
  }

  .project-content {
    position: absolute;
    inset: 0;

    display: flex;
    flex-direction: column;
    justify-content: space-between;

    padding: 24px;
  }

  .project-number {
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.08em;

    opacity: 0.72;
  }

  .project-bottom {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;

    gap: 16px;
  }

  .project-bottom h3 {
    margin: 0;

    font-size: clamp(23px, 2vw, 34px);
    line-height: 1;
    letter-spacing: -0.035em;

    font-weight: 500;
  }

  /* Większy tytuł na głównym kaflu */
  .project-card--hero .project-bottom h3 {
    font-size: clamp(34px, 3.4vw, 52px);
  }

  .project-link {
    display: flex;
    align-items: center;
    gap: 10px;

    flex-shrink: 0;
    white-space: nowrap;

    font-size: 14px;
    font-weight: 500;

    opacity: 0;
    transform: translateY(10px);

    transition:
      opacity 0.35s ease,
      transform 0.35s ease;
  }

  .project-card:hover .project-link {
    opacity: 1;
    transform: translateY(0);
  }

  .project-arrow {
    display: flex;
    align-items: center;
    justify-content: center;

    width: 34px;
    height: 34px;

    border: 1px solid rgba(255, 255, 255, 0.45);
    border-radius: 50%;

    font-size: 15px;
  }

  /* =========================
     TABLET
     ========================= */

  @media (max-width: 1100px) {
    .projects-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      grid-template-rows: none;
      grid-auto-rows: 280px;
    }

    .project-card--hero,
    .project-card--bottom,
    .projects-grid .project-card:nth-child(2),
    .projects-grid .project-card:nth-child(3),
    .projects-grid .project-card:nth-child(4),
    .projects-grid .project-card:nth-child(5) {
      grid-column: auto;
      grid-row: auto;
    }

    .project-card--hero .project-bottom h3 {
      font-size: clamp(24px, 3vw, 34px);
    }
  }

  /* =========================
     MOBILE
     ========================= */

  @media (max-width: 900px) {
    .projects-section {
      padding: 80px 0;
    }

    .projects-container {
      width: min(100% - 36px, 1400px);
    }

    .projects-heading {
      margin-bottom: 42px;
    }

    .projects-heading h2 {
      font-size: clamp(42px, 11vw, 68px);
    }

    .projects-grid {
      grid-template-columns: 1fr;
      grid-template-rows: none;
      grid-auto-rows: auto;

      gap: 14px;
    }

    .project-card {
      min-height: 360px;
    }

    .project-content {
      padding: 24px;
    }

    .project-link {
      opacity: 1;
      transform: none;
    }
  }

  @media (max-width: 520px) {
    .project-card {
      min-height: 340px;
    }

    .project-bottom {
      display: block;
    }

    .project-bottom h3 {
      margin-bottom: 16px;
    }
  }
</style>