<header class="navbar">
  <a class="navbar__logo" href="/" aria-label="Studio 306 — strona główna">
    <img src="/public/images/logo.png" alt="Studio 306" />
  </a>

  <nav class="navbar__nav" aria-label="Główna nawigacja">
    <a href="/#oferta">Oferta</a>
    <a href="/#realizacje">Realizacje</a>
    <a href="/#o-nas">O nas</a>
    <a href="/#kontakt">Kontakt</a>
  </nav>

  <a class="navbar__cta" href="#kontakt">
    Wyceń projekt
  </a>
</header>

<style>
  .navbar {
    position: relative;
    z-index: 100;

    width: 100%;
    height: 96px;

    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;

    padding: 0 clamp(32px, 4vw, 64px);

    background: #fff;
    border-bottom: 1px solid #ececec;

    box-sizing: border-box;
  }

  .navbar__logo {
    display: flex;
    align-items: center;
  }

  .navbar__logo img {
    width: 42px;
    height: 42px;
    display: block;
  }

  .navbar__nav {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 42px;
  }

  .navbar__nav a {
    color: #111;
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    transition: opacity .2s;
  }

  .navbar__nav a:hover {
    opacity: .55;
  }

  .navbar__cta {
    justify-self: end;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 12px 24px;

    border: 2px solid #111;
    border-radius: 999px;

    color: #111;
    text-decoration: none;
    font-weight: 600;

    white-space: nowrap;

    transition: .2s;
  }

  .navbar__cta:hover {
    background: #111;
    color: #fff;
  }

  @media (max-width: 768px) {

    .navbar {
      grid-template-columns: auto auto;
      padding: 0 24px;
    }

    .navbar__nav {
      display: none;
    }

    .navbar__logo img {
      width: 36px;
      height: 36px;
    }

    .navbar__cta {
      padding: 10px 18px;
      font-size: .9rem;
    }

  }
</style> 